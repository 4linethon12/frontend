import styled from 'styled-components';

export const LoginBackground = styled.div`
width: 100vw;
  min-height: 100vh; 
  overflow: hidden;
  background: linear-gradient(
    180deg,
    #0c1929 0%,
    #184e40 100%
  );
  display: flex;
  justify-content: center;
  flex-direction: column;
  align-items: center;
  border-radius: 8px; /* 필요한 경우 추가 */
`;
